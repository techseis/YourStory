@model SubscriptionAddressModel

@{
    //page title
    ViewBag.Title = T("Admin.Subscriptions.Address.EditAddress").Text;
    //active menu item (system name)
    Html.SetActiveMenuItemSystemName("Subscriptions");
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div class="content-header clearfix">
        <h1 class="pull-left">
            @T("Admin.Subscriptions.Address.EditAddress") 
            <small><i class="fa fa-arrow-circle-left"></i>@Html.ActionLink(T("Admin.Subscriptions.Address.BackToSubscription").Text, "Edit", new { id = Model.SubscriptionId })</small>
        </h1>
        <div class="pull-right">
            <button type="submit" name="save" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Admin.Common.Save")
            </button>
        </div>
    </div>
    @Html.Partial("_CreateOrUpdateAddress", Model)
}
