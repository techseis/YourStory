@model ArticleDetailsModel
@using YStory.Web.Models.Catalog;
@if (Model.EmailAFriendEnabled)
{
    <div class="email-a-friend">
        <input type="button" value="@T("Articles.EmailAFriend")" class="button-2 email-a-friend-button" onclick="setLocation('@Url.RouteUrl("ArticleEmailAFriend", new { articleId = Model.Id })')" />
    </div>
}
