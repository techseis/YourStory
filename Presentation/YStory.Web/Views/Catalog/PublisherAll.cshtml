@model IList<PublisherModel>
@{
    Layout = "~/Views/Shared/_ColumnsTwo.cshtml";

    //title
    Html.AddTitleParts(T("PageTitle.Publishers").Text);
    //page class
    Html.AppendPageCssClassParts("html-publisher-list-page");
}
@using YStory.Web.Models.Catalog;
<div class="page publisher-list-page">
    <div class="page-title">
        <h1>@T("Publishers.List")</h1>
    </div>
    <div class="page-body">
        <div class="publisher-grid">
            <div class="item-grid">
                @foreach (var item in Model)
                {
                    <div class="item-box">
                        <div class="publisher-item">
                            <h2 class="title">
                                <a href="@Url.RouteUrl("Publisher", new { SeName = item.SeName })" title="@item.PictureModel.Title">
                                    @item.Name
                                </a>
                            </h2>
                            <div class="picture">
                                <a href="@Url.RouteUrl("Publisher", new { SeName = item.SeName })" title="@item.PictureModel.Title">
                                    <img alt="@item.PictureModel.AlternateText" src="@item.PictureModel.ImageUrl" title="@item.PictureModel.Title" />
                                </a>
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
</div>
